{
  "name": "nantianmen-cli",
  "version": "0.4.21",
  "description": "Nantianmen CLI - command line tool (Go binary in v0.4.21+; legacy Node entry still ships as fallback)",
  "type": "module",
  "bin": { "nantianmen": "./index.js" },
  "scripts": {
    "cli:build": "cd cli && bun build ./index.js --compile --target=bun-windows-x64 --outfile=../releases/nantianmen-cli-0.4.21-win-x64.exe && cp -r ../server ../releases/server"
  }
}
